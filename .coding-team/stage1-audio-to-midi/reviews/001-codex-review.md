1. Change the checkpoint load path to fail on any state-dict mismatch instead of silently accepting partial loads. In [src/gtp/model/kong.py](/Users/max/Documents/Programming/AI/Archived/R2T/rec2tabs-core/src/gtp/model/kong.py), [src/gtp/inference.py](/Users/max/Documents/Programming/AI/Archived/R2T/rec2tabs-core/src/gtp/inference.py), and [scripts/test_model_load.py](/Users/max/Documents/Programming/AI/Archived/R2T/rec2tabs-core/scripts/test_model_load.py), remove the `strict=False` behavior or explicitly assert there are no missing/unexpected keys. This matters because the task’s acceptance criterion is that the vendored architecture still matches the pretrained checkpoint exactly; the current code can print “loaded successfully” even if some weights were skipped.

2. Fix output trimming in `PianoTranscription.transcribe` so it crops by frame count, not by raw sample count. In [src/gtp/inference.py](/Users/max/Documents/Programming/AI/Archived/R2T/rec2tabs-core/src/gtp/inference.py), the loop that does `self._deframe(output_dict[key])[:audio_len]` is using `audio_len` in samples against arrays whose first dimension is model frames, so padded inputs will return too many predictions for the padded tail. Compute the expected original frame length from the audio duration / hop size (or restore Kong’s original bookkeeping) and crop to that instead.